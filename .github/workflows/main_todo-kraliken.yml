# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Deploy Next.js to Azure Web App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js (production)
        env:
          NODE_ENV: production
        run: npm run build

      - name: Prepare release artifacts
        run: |
          mkdir -p release
          cp -R .next release/.next
          cp -R public release/public 2>/dev/null || true
          [ -f next.config.js ] && cp next.config.js release/ || true
          [ -f server.js ] && cp server.js release/ || true
          cp package.json package-lock.json release/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nextjs-release
          path: release

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: nextjs-release
          path: release

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_38094B7437AB4EAA86CC41C41735B70D }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_CC255DEC69F74D4B870C6DD3BD176C26 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICESUBSCRIPTIONID_4191902740064040892DE55C300BE39C }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: todo-kraliken
          slot-name: Production
          package: release
